{% extends "/frontoffice/layout.html.twig" %}

{% block title %}Tous les posts{% endblock %}

{% block banner %}
    <img id="headband" src="images/headband.jpg" alt="Bannière : Figurines de Mario, Luigi et Yoshi" />
{% endblock %}

{% block content %}
<section id='latest_reviews'>
    <h2>LES DERNIERES REVIEWS</h2>
        <div id="cards">
            {% for post in posts %}
                <section class="card">
                    <a href='index.php?action=review&id={{ post.id }}'><img src= 'images/{{ post.image }}' alt= {{ post.image }} class="thumbnail" /></a>
                    <div class='thumbnail_infos'>
                        <h3 class="thumbnail_title">{{ post.reviewTitle }}</h3>
                        <div class="author_comments">
                            <span class='pseudo_date'>{{ post.reviewer }} | {{ post.reviewDate }}</span>
                            <span class="comments_number"><i class="fas fa-comments"></i>
                                12
                            </span>
                        </div>
                    </div>
                </section>
            {% endfor %}
        </div>
    <button class="show_all"><a href='index.php?action=all_reviews&page=1'>TOUTES LES REVIEWS</a></button>
</section>
{% endblock %}
