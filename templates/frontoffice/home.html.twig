{% extends "/frontoffice/layout.html.twig" %}

{% block title %}Accueil{% endblock %}

{% block banner %}
    <img id="headband" src="images/headband.jpg" alt="Bannière : Figurines de Mario, Luigi et Yoshi" />
{% endblock %}
{% block sectionTitle %}<h1 id='section_title'>LES DERNIERES REVIEWS</h1>{% endblock %}

{% block content %}

<section id='latest_reviews'>
        <div id="cards">
            {% for post in posts %}
                <section class="card">
                    <span class="api_game_id">{{ post.apiGameId }}</span>
                    <a href='index.php?action=review&id={{ post.reviewId }}'><img class="thumbnail_image_home" /></a>
                    <div class='thumbnail_infos'>
                        <h3 class="thumbnail_title">{{ post.reviewTitle|raw }}</h3>
                        <div class="author_comments">
                            <span class='pseudo_date'>Postée par : {{ post.username }}, le {{ post.reviewDate|date('d/m/Y - H:i:s') }}</span>
                        </div>
                    </div>
                </section>
            {% endfor %}
        </div>
    <button class="show_all"><a href='index.php?action=all_reviews&page=1'>TOUTES LES REVIEWS</a></button>
</section>
{% endblock %}
